(async (url) => { const r = await fetch(url, { credentials: 'include' }); if (r.status !== 200) return { status: r.status }; const b = await r.blob(); const d = await new Promise((res, rej) => { const fr = new FileReader(); fr.onload = () => res(fr.result); fr.onerror = () => rej(fr.error); fr.readAsDataURL(b); }); return { status: r.status, type: b.type, size: b.size, data: d.slice(d.indexOf(',') + 1) }; })